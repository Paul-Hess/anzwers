import { userValidate } from 'q-and-a/helpers/user-validate';
import { module, test } from 'qunit';

module('Unit | Helper | user validate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userValidate([42]);
  assert.ok(result);
});
